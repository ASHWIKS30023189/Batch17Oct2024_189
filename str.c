#include<stdio.h>
int main()
{
	char empName[20];
		fgets(empName,19,stdin);
		puts(empName);
		/*
		if(strcmp(gets(buff)=="quit")==0)
		{
			quit code
		}
		else{
			other than quit code
		}
		i*/
		return 0;
}

